class DataControllerValidations {
    constructor(/*rendererRouter, renderRequest, santaPath*/) {
        // this.rendererRouter = rendererRouter
        // this.renderRequest = renderRequest
        // this.santaPath = santaPath
        //
        // this.renderer = this.rendererRouter.rendererFor(renderRequest)
    }

    async validate(collectionName, filter, sort, skip, limit) {
        // validate all properties
        // return this.renderer.getWixCodeSeoData(this.renderRequest, this.santaPath)
    }
}