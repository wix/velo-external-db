{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna exec \"npm test\"",
    "release": "lerna version --no-push",
    "ci:publish": "lerna publish -y from-git"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": {
    "packages": [
      "velo-demo",
      "velo-external-db"
    ]
  }
}
